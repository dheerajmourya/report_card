# Django Report Card System

A student report card manager with HTML form + PDF generator.

## Run Instructions
1. Clone repo
2. Create virtualenv
3. Run: `pip install -r requirements.txt`
4. Run: `python manage.py migrate`
5. Run: `python manage.py runserver`
